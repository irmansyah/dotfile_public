# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
# i3 config file (v4)
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:FiraCode Nerd Font Mono 0

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

floating_modifier $mod

# exec --no-startup-id xmodmap ~/.config/key-map/key-remap.Xmodmap
# exec --no-startup-id xmodmap ~/.config/key-map/swap_caps_ctrl.Xmodmap

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status

bindsym $mod+g exec --no-startup-id ~/.config/key-map/volume_limit.sh
bindsym $mod+b exec --no-startup-id ~/.config/key-map/volume_lower.sh

bindsym XF86Battery exec ~/.local/bin/battery-notify.sh

# bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

bindsym $mod+Shift+g exec brightnessctl set +5%
bindsym $mod+Shift+b exec brightnessctl set 5%-

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
# tiling_drag modifier titlebar

# start a terminal
bindsym $mod+Return exec ghostty

# kill focused window
bindsym $mod+Shift+q kill

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right
bindsym $mod+Tab focus left

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

bindsym $mod+Shift+v split h
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym --release $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
# bindsym $mod+w layout tabbed

# bindsym $mod+w layout stacking
bindsym --release $mod+e layout toggle split
# bindsym $mod+e layout splitv

# toggle tiling / floating
bindsym --release $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym --release $mod+s focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
  bindsym h resize shrink width 1 px or 1 ppt
  bindsym j resize grow height 1 px or 1 ppt
  bindsym k resize shrink height 1 px or 1 ppt
  bindsym l resize grow width 1 px or 1 ppt

  # same bindings, but for the arrow keys
  bindsym Left resize shrink width 1 px or 1 ppt
  bindsym Down resize grow height 1 px or 1 ppt
  bindsym Up resize shrink height 1 px or 1 ppt
  bindsym Right resize grow width 1 px or 1 ppt



  # back to normal: Enter or Escape or $mod+r
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym $mod+r mode "default"
}

mode "resize-20" {
  bindsym h resize shrink width 20 px or 20 ppt
  bindsym j resize grow height 20 px or 20 ppt
  bindsym k resize shrink height 20 px or 20 ppt
  bindsym l resize grow width 20 px or 20 ppt

  # same bindings, but for the arrow keys
  bindsym Left resize shrink width 20 px or 20 ppt
  bindsym Down resize grow height 20 px or 20 ppt
  bindsym Up resize shrink height 20 px or 20 ppt
  bindsym Right resize grow width 20 px or 20 ppt

  # back to normal: Enter or Escape or $mod+r
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym $mod+Shift+u mode "default"
}

bindsym $mod+r mode "resize-20"
bindsym $mod+Shift+u mode "resize"

bar {
	status_command i3status
}

# window colours: border background text
client.focused             #9e7f35 #9e7f35 #9e7f35 #0AAEB3
# client.placeholder         #BAA67F #BAA67F #BAA67F
client.urgent              #0F0F0F #BC9B54 #E5E5E5 #0AAEB3

# Map Caps Lock to Hyper
set $hyper Hyper_L

# Map Hyper to Escape
bindsym --release $hyper exec xdotool keyup Escape; xdotool keydown Escape

# Hyper keybindings for arrow keys
bindsym --release $hyper+h exec xdotool key --clearmodifiers Left
bindsym --release $hyper+j exec xdotool key --clearmodifiers Down
bindsym --release $hyper+k exec xdotool key --clearmodifiers Up
bindsym --release $hyper+l exec xdotool key --clearmodifiers Right

# Floating windows only
for_window [class="Chromium"] floating enable, resize set 900px 1000px, move scratchpad, scratchpad show
for_window [class="Postman"] floating enable, resize set 1200px 1000px, move scratchpad, scratchpad show
for_window [class="Pcmanfm"] floating enable, resize set 1200px 1000px, move scratchpad, scratchpad show
for_window [class="code-oss"] floating enable, resize set 1600px 1400px, move scratchpad, scratchpad show
for_window [class="firefox" instance="Places"] floating enable, resize set 1200px 1000px, move scratchpad, scratchpad show
for_window [class="zen-beta" instance="Places"] floating enable, resize set 1200px 1000px, move scratchpad, scratchpad show
for_window [class="pavucontrol"] floating enable, resize set 1200px 1000px
for_window [class="WORK_DOC"] floating enable, resize set 1201px 1000px, move absolute position center, move scratchpad, scratchpad show
for_window [class="WORK_SSH"] floating enable, resize set 1200px 1000px, move absolute position center, move scratchpad, scratchpad show
for_window [class="scrcpy"] floating enable, resize set 490px 1120px, move scratchpad, scratchpad show
for_window [class="Gpick"] floating enable, resize set 800px 600px, move scratchpad, scratchpad show
for_window [class="TelegramDesktop"] floating enable, resize set 1600px 1000px, move scratchpad, scratchpad show
for_window [class="TelegramDesktop"] border normal
for_window [class="ghostty"] border normal

for_window [title="Emulator"] floating enable, resize set 1600px 1000px, move scratchpad, scratchpad show

# Quit Window
# bindsym $mod+Shift+q exec --no-startup-id ~/.config/i3/scripts/confirm_quit_window.sh

# Window Floating
bindsym --release $mod+a exec ~/.config/i3/scripts/floating_hidden_toggle.sh

# Screenshot
bindsym --release $mod+p exec ~/.config/scrot/generate_scrot.sh

# Start Firefox
# bindsym $mod+Shift+f exec firefox

bindsym $mod+i exec hints

# Rofi run
bindsym $mod+space exec rofi -show drun -run-shell-command
# bindsym $mod+space exec --no-startup-id i3-dmenu-desktop

# Lock Screen
# bindsym $mod+Shift+z exec i3lock -c 282828 && systemctl suspend
bindsym $mod+Shift+z exec ~/.config/i3/scripts/lock.sh && systemctl suspend

# Toggle layout
bindsym $mod+w exec ~/.config/i3/scripts/toggle_layout.sh

# Show Grid Layer
bindsym $mod+n exec echo "cursor_mover_show" | socat - UNIX-CONNECT:/tmp/cursor_mover_socket

# bindsym $mod+d exec pcmanfm > /dev/null 2>&1 & 

# Start Some App
exec --no-startup-id cursor_mover
exec --no-startup-id picom --config ~/.config/picom/picom.conf
exec --no-startup-id nitrogen --restore
exec --no-startup-id python ~/.config/i3/scripts/i3-autosplit/i3-autosplit.py
exec --no-startup-id ~/.config/i3/scripts/display-input.sh
exec --no-startup-id ~/.config/key-map/touchpad_settings.sh
exec --no-startup-id ~/.config/polybar/scripts/launch.sh
exec --no-startup-id /usr/local/bin/touchpad-drag.sh
exec --no-startup-id xrandr --output DP-1 --mode 2560x1440 --pos 0x0 --primary --output eDP-1 --mode 1920x1080 --pos 2560x0
